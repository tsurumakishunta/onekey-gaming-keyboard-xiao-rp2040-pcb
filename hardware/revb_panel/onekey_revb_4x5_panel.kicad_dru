(version 1)

(rule "Panel copper clearance"
  (constraint clearance (min 0.25mm)))

(rule "Panel copper to edge"
  (constraint edge_clearance (min 0.30mm)))

(rule "Minimum track width"
  (constraint track_width (min 0.25mm)))
